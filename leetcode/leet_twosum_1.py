class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for idx in range(len(nums)-1):
            for j in range(len(idx+1, len(nums)):
                if target - nums[idx] == nums[j]:
                    return [idx, j]   
